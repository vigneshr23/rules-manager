const preConditions = ["If", "If...And", "If...Or"];
const postConditions = ["And", "Or"];
const entityTypes = [
  { type: "amount", dataType: Number },
  { type: "age", dataType: Number },
];

export { preConditions, postConditions, entityTypes };
